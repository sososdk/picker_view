name: picker_view_example
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  picker_view:
    path: ../
  http: ^0.12.2
  auto_size_text: ^2.1.0
  device_preview:
    git:
      url: https://github.com/aloisdeniel/flutter_device_preview.git
      path: ./device_preview

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
